mixin createTree(listParent,level = 1)
    each item in listParent 
        - const arr = new Array(level + 1).join("-- ")
        option(value=item.id) #{arr} #{item.title} 
        if item.children && item.children.length > 0 
            +createTree(item.children, level + 1)
            
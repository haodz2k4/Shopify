nav.navbar.navbar-expand-lg.navbar-light.bg-light
      if user.fullName
        a.navbar-brand(href='#') #{user.fullName} 
        
      else 
        a.navbar-brand(href='#') Admin Panel 
      if user.avatar
        a(href="/admin/my-account") 
          img(src=user.avatar, alt="", style="width: 30px; height: 30px; border-radius: 30px;")
        
      else 
        a(href="/admin/my-account") 
          img(src="/admin/images/logo_default.png", alt="", style="width: 30px; height: 30px; border-radius: 30px;")
        p #{localRoles.title} 
      button.navbar-toggler(type='button', data-toggle='collapse', data-target='#navbarNav', aria-controls='navbarNav', aria-expanded='false', aria-label='Toggle navigation')
        span.navbar-toggler-icon
      #navbarNav.collapse.navbar-collapse
        ul.navbar-nav.ml-auto
          li.nav-item.active
            a.nav-link(href='/admin/dashboard')
              | Home 
              span.sr-only (current)
          li.nav-item
            a.nav-link(href='#') Dashboard
          li.nav-item
            button.nav-link(btn-logout) Logout 